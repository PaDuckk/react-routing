import React from "react";
import styled from "styled-components";

type Props = {};

const Root = (props: Props) => {
  return <Wrapper>Root</Wrapper>;
};

export default Root;

const Wrapper = styled.div`
  padding: 36px 48px;
  font-size: 24px;
  color: black;
`;
